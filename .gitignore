# Root-level tooling (Supabase CLI devDependency — see package.json)
node_modules/

# Supabase local/dev artifacts
supabase/.branches
supabase/.temp
supabase/.env.keys
supabase/.env.local
supabase/.env.*.local

# Local resource/asset prep (Urdu audio-splitting pipeline and any other
# raw/large working material under resources/) — never pushed; not part
# of any app's build. Large outputs get uploaded straight to hosting
# (e.g. Supabase Storage) separately, per that pipeline's own plan.
/resources/

# OS
.DS_Store
Thumbs.db

# Claude Code local/personal settings (machine-specific permission overrides)
.claude/settings.local.json
